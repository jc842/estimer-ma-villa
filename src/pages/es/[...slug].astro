---
import Layout from '../../layouts/Layout.astro';
import { getCollection, render } from 'astro:content';

export async function getStaticPaths() {
  const posts = await getCollection('blog', ({ data }) => data.lang === 'es');
  return posts.map(post => ({
    params: { slug: post.id.replace(/^es\//, '') },
    props: { post },
  }));
}

const { post } = Astro.props;
const { Content } = await render(post);
---

<Layout title={post.data.title} description={post.data.description} lang="es">
  <article class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
    <div class="mb-8">
      <a href="/es/" class="text-amber-800 hover:underline text-sm font-semibold inline-flex items-center gap-1 mb-4">
        &larr; Volver a las Guías de Tasación
      </a>
      <h1 class="text-3xl sm:text-4xl font-extrabold text-stone-900 tracking-tight mb-4">
        {post.data.title}
      </h1>
      <p class="text-lg text-stone-600">{post.data.description}</p>
      <div class="flex items-center gap-4 text-sm text-stone-500 mt-4 pt-4 border-t border-stone-200">
        <span>Disciplina: <strong class="text-stone-800">{post.data.category}</strong></span>
        <span>•</span>
        <span>{new Date(post.data.pubDate).toLocaleDateString('es-ES', { year: 'numeric', month: 'long', day: 'numeric' })}</span>
      </div>
    </div>

    <div class="prose prose-stone max-w-none">
      <Content />
    </div>
  </article>
</Layout>
